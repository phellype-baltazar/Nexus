"use client";
import {useState} from "react";import {createClient} from "@/lib/supabase/client";
export function LoginForm(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[busy,setBusy]=useState(false);const[message,setMessage]=useState<string|null>(null);const supabase=createClient();
async function oauth(provider:"google"|"azure"){setBusy(true);const redirectTo=`${window.location.origin}/auth/callback`;const options=provider==="azure"?{redirectTo,scopes:"email"}:{redirectTo};const{error}=await supabase.auth.signInWithOAuth({provider,options});if(error){setMessage(error.message);setBusy(false)}}
async function emailLogin(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage(null);const{error}=await supabase.auth.signInWithPassword({email,password});if(error){setMessage(error.message);setBusy(false);return}window.location.href="/app/dashboard"}
return <div className="form"><button className="btn btn-outline" disabled={busy} onClick={()=>oauth("google")}>Continuar com Google</button><button className="btn btn-outline" disabled={busy} onClick={()=>oauth("azure")}>Continuar com Microsoft</button><div className="divider">ou</div><form className="form" onSubmit={emailLogin}><div className="field"><label htmlFor="email">E-mail</label><input id="email" className="input" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label htmlFor="password">Senha</label><input id="password" className="input" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn btn-primary" disabled={busy}>{busy?"Entrando...":"Entrar"}</button></form>{message&&<p className="muted" role="alert">{message}</p>}</div>}
