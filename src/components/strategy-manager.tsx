"use client";
import {useState} from "react";
import {createClient} from "@/lib/supabase/client";
export function StrategyManager({organizationId,groups}:{organizationId:string;groups:any[]}) {
  const [name,setName]=useState(""),[description,setDescription]=useState(""),[group,setGroup]=useState(""),[start,setStart]=useState(""),[end,setEnd]=useState(""),[msg,setMsg]=useState("");
  async function submit(e:React.FormEvent){e.preventDefault();const s=createClient();const{error}=await s.from("strategic_objectives").insert({organization_id:organizationId,group_id:group||null,name:name.trim(),description:description||null,start_date:start||null,due_date:end||null});if(error)setMsg(error.message);else location.reload()}
  return <form className="card form" onSubmit={submit}><h2>Novo objetivo estratégico</h2><div className="field"><label>Objetivo</label><input className="input" value={name} onChange={e=>setName(e.target.value)} required/></div><div className="field"><label>Grupo responsável</label><select className="select" value={group} onChange={e=>setGroup(e.target.value)}><option value="">Empresa</option>{groups.map(g=><option key={g.id} value={g.id}>{g.name}</option>)}</select></div><div className="field"><label>Descrição</label><textarea className="textarea" value={description} onChange={e=>setDescription(e.target.value)}/></div><div className="grid grid-2"><div className="field"><label>Início</label><input className="input" type="date" value={start} onChange={e=>setStart(e.target.value)}/></div><div className="field"><label>Fim</label><input className="input" type="date" value={end} onChange={e=>setEnd(e.target.value)}/></div></div><button className="btn btn-primary btn-block">Criar objetivo</button>{msg&&<div className="error">{msg}</div>}</form>
}
