import {createClient} from "@/lib/supabase/server";import {getCurrentWorkspace} from "@/lib/workspace";
export default async function Page(){const s=await createClient();const w=await getCurrentWorkspace();if(!w)return null;const{data}=await s.from("audit_logs").select("id,action,entity_type,created_at").eq("organization_id",w.id).order("created_at",{ascending:false}).limit(100);return <main className="page"><span className="eyebrow">Governança</span><h1>Auditoria</h1><section className="card list">{!data?.length?<div className="empty">Nenhum registro.</div>:data.map(a=><div className="row" key={a.id}><div className="row-main"><div className="row-title">{a.action}</div><div className="row-sub">{a.entity_type} · {new Date(a.created_at).toLocaleString("pt-BR")}</div></div></div>)}</section></main>}
