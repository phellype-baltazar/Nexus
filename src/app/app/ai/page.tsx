import {createClient} from "@/lib/supabase/server";import {getCurrentWorkspace} from "@/lib/workspace";import {AiRequestForm} from "@/components/ai-request-form";
export default async function Page(){const s=await createClient();const w=await getCurrentWorkspace();if(!w)return null;const[{data:cases},{data:jobs}]=await Promise.all([s.from("ai_use_cases").select("key,name").eq("is_active",true).order("name"),s.from("ai_jobs").select("id,use_case_key,status,created_at").eq("organization_id",w.id).order("created_at",{ascending:false}).limit(10)]);return <main className="page"><span className="eyebrow">Inteligência</span><h1>IA</h1><p className="muted">Análises assistidas com revisão humana.</p><AiRequestForm organizationId={w.id} useCases={cases||[]}/><div className="section-title"><h2>Solicitações recentes</h2></div><section className="card list">{!jobs?.length?<div className="empty">Nenhuma solicitação ainda.</div>:jobs.map(j=><div className="row" key={j.id}><div className="row-main"><div className="row-title">{j.use_case_key}</div><div className="row-sub">{new Date(j.created_at).toLocaleString("pt-BR")}</div></div><span className="chip">{j.status}</span></div>)}</section></main>}
