import {LoginForm} from "@/components/login-form";
export default async function LoginPage({searchParams}:{searchParams:Promise<{next?:string}>}){const{next=""}=await searchParams;return <main className="login-wrap"><section className="card login-card"><div className="hero-logo">N</div><span className="eyebrow">Nexus</span><h1>Gestão conectada, do plano à execução.</h1><p className="muted">Entre para acessar suas organizações, programas, projetos e atividades.</p><LoginForm nextPath={next}/></section></main>}
